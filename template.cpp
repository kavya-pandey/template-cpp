#include<bits/stdc++.h>
using namespace std;

// ¯\_(ツ)_/¯

typedef long long ll;
typedef std::vector<int> veci;
typedef std::vector<char> vecc;
typedef std::set<int> seti;
typedef std::set<char> setc;
// #define lll __int128_t
#define cnew cout<<"\n"
#define cyes cout<<"YES\n"
#define cno cout<<"NO\n"
#define minf INT_MIN;
#define maxf INT_MAX;
#define repi(a,b) for(int i=a;i<b;i++)
#define repij(a,b) for(int j=a;j<b;j++)
#define repd(a,b) for(int i=a;i>b;i--)
#define max_(a,b) (a>b ? a:b)
#define min_(a,b) (a<b ? a:b)
#define min_vec(a)	*min_element(a.begin(), a.end())	
#define max_vec(a)	*max_element(a.begin(), a.end())	

void test()
{
	
}

int main()
{
	// freopen("input.txt", "r", stdin);
	// freopen("output.txt", "w", stdout);
	ios_base::sync_with_stdio(NULL); cin.tie(NULL); cout.tie(NULL);
	int t=1;	cin>>t;
	while(t--)
		test();
	return 0;
}
